export default class SocketIOConstant {
    /**
     * Once a connection incoming, the server would be ask the connection's
     * identity. This parameter indicates the maximum waiting time to the
     * authorization period. (in milliseconds)
     */
    public static authorizationTimeout = 3 * 1000;
}
